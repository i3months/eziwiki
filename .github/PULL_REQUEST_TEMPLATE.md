## What changed

<!-- One or two sentences. The commit messages carry the why; this is the summary a reviewer reads first. -->

## Checklist

- [ ] `npm run lint`, `npm run type-check` and `npm test` pass
- [ ] `npm run build` passes (it runs `check:links`, which is strict in CI)
- [ ] New interface text is added to `lib/i18n/strings.ts` in both languages, not written inline
- [ ] User-facing changes are noted in `CHANGELOG.md` under *Unreleased*
- [ ] A change to `payload/config.ts` keys, frontmatter or `_meta.json` is documented in the README
